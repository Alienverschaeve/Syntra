<?php
$tekst= "Where there is a will, there is a way \n Donde hay un voluntad hay un camino";
$integer = 1234;
Echo strlen($tekst);// dit bepaalt de lengte van je string

Echo str_repeat();
    Echo str_replace();
    Echo strpos($tekst," "); // zoekt iets in een bepaalde string
Echo strpos($tekst, " ", 20); // gaat zoeken vanaf het einde(opgegeven)
    Echo number_format(); // afronden tot bepaalde decimalen (string)
        Echo round(); //afronden tot op decimalen
        Echo intdiv(1,2); // Geeft je het gehele getal van een deling (geen rest of na de komma)
        Echo int(); //string omzetten naar integer
        Echo $modulo=1%3; // Geeft je de restwaarde van een deling (alles na de komma)
Echo ucfirst($tekst); // Zet de eerste letter in Capital
Echo ceil ($integer); // rond een nummer af naar boven
Echo floor($integer; // rond altijd af naar beneden)
Split
join
replace
;