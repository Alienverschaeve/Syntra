
  // 4.  Write a JavaScript function to compute the sum of an array of integers.
  function SumInteger(a,b) => {
      let array[];
    return SumInteger.forEach(
        {Sum=a+b}
    ) 
    Array=+sum;
    })
}

const result = names.find(name => name === "Alex");
